[project]
name = "calculator"  # Component-specific name
version = "0.1.0"
description = "Calculator component for technology-template-repository."
requires-python = ">=3.10.9, <3.11"

# Only include tool configurations if they're different from the root