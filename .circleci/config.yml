version: 2.1

orbs:
  python: circleci/python@2.1

jobs:
  build-and-test:
    docker:
      - image: cimg/python:3.10
    steps:
      - checkout

      - run:
          name: Install uv
          command: curl -LsSf https://astral.sh/uv/install.sh | sh

      - run:
          name: Create and Activate Virtual Environment
          command: |
            uv venv .venv
            echo 'export PATH=".venv/bin:$PATH"' >> $BASH_ENV
            echo "export PYTHONPATH=$PWD/src" >> $BASH_ENV  # Ensure src/ is recognized
            source .venv/bin/activate

      - run:
          name: Install Dependencies
          command: |
            uv pip install ".[dev]"
            uv pip install mypy ruff pytest pytest-cov  # Ensure all dependencies

      - run:
          name: Run Static Analysis
          command: |
            mypy src tests
            ruff check . --config pyproject.toml
            
      # Run all unit tests first with coverage
      - run:
          name: Run Unit Tests
          command: |
            nose2 --with-coverage --coverage-report xml --coverage=src --plugin nose2.plugins.junitxml --junit-xml=test-results/unit.xml discover src/calculator/test src/logger/test src/notifier/test

      # Run integration tests and append coverage
      - run:
          name: Run Integration Tests
          command: |
            COVERAGE_FILE=.coverage.integration nose2 --with-coverage --coverage=src --plugin nose2.plugins.junitxml --junit-xml=test-results/integration.xml discover tests/integration
            coverage combine .coverage .coverage.integration

      # Run e2e tests and generate final coverage report
      - run:
          name: Run End-to-End Tests and Check Coverage
          command: |
            COVERAGE_FILE=.coverage.e2e nose2 --with-coverage --coverage=src --plugin nose2.plugins.junitxml --junit-xml=test-results/e2e.xml discover tests/e2e
            coverage combine .coverage .coverage.integration .coverage.e2e
            coverage report --fail-under=70
            coverage xml
            coverage html

      - store_test_results:
          path: test-results

      - store_artifacts:
          path: htmlcov
          destination: coverage-report

workflows:
  main:
    jobs:
      - build-and-test
